// LoginVM.cs (может быть пустым или упрощённым)
using SeaBattleWPF.mvvm;

namespace SeaBattleWPF.VM
{
    public class LoginVM : BaseVM
    {
        // Этот класс теперь почти не нужен, 
        // так как логика переехала в LoginPage.xaml.cs
        // Но оставляем пустым для совместимости
    }
}